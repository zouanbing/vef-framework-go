# VEF Framework Go — Agent Guide

The authoritative, self-contained agent guide for this repository is [CLAUDE.md](CLAUDE.md). Read that file before touching code — it covers architecture, conventions, workflow requirements, and gotchas for every agent, regardless of vendor.

This file is intentionally just a pointer: maintaining two copies of the guide let them drift, so all content lives in `CLAUDE.md` only.
